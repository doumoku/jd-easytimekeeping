/**
 * Defines the constants which are used in multiple places for time calculations.
 */

export const Constants = {
    minutesPerDay: 60 * 24,
    shiftsPerDay: 4,
    hoursPerShift: 6,
    hoursPerDay: 24,
    minutesPerShift: 60 * 6, // 6 hours * 60 minutes per hour
}
